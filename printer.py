def invalid(prefix, actions):
    print "{0} Sorry, I didn't understand that. Valid options are: {1}".format(prefix, ", ".join(actions))

def p(prefix, words):
    print "{0} {1}".format(prefix, words)
